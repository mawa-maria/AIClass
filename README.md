Sur une autre machine, pour recréer l'environnement :

bash:
git clone votre-repo
python -m venv venv
source venv/bin/activate  # ou .\venv\Scripts\activate sur Windows
pip install -r requirements.txt
